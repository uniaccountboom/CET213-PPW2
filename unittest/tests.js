 QUnit.test('isPrime', function (assert) {
     assert.equal(isPrime(6), false, 'Should return false');
     });

     QUnit.test('isPrime', function (assert) {
        assert.equal(isPrime(3), true, 'Should return true');
        });
    